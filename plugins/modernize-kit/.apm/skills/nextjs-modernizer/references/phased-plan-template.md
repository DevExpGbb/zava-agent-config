# MIGRATION-PLAN.md template

The skill writes this file at the consumer repo's root after step 5. Substitute `{...}` placeholders with real values.

---

# Next.js 14 → 15 Migration Plan

> Auto-generated by `nextjs-modernizer` skill on `{ISO date}`.
> Source codebase: `{repo path}` (next `{detected version}`, react `{detected version}`)
> Catalog: [`next-14-to-15-breaking-changes.md`](https://github.com/DevExpGbb/zava-agent-config/blob/main/plugins/modernize-kit/.apm/skills/nextjs-modernizer/references/next-14-to-15-breaking-changes.md)

## Summary

| Class | Findings |
|---|---|
| SAFE (informational) | `{N}` |
| AUTOFIX (applied by this skill) | `{M}` |
| MANUAL (team must edit) | `{K}` |
| **Total** | `{N+M+K}` |

---

## Phase 1 — Autofixed (this skill)

The skill applied the following changes. **Review the diff before merging.**

| Pattern | File | Line | Change |
|---|---|---|---|
| `{BC-NNN}` | `{path}` | `{N}` | `{before → after}` |
| ... | | | |

If empty, the skill found no AUTOFIX-class patterns.

---

## Phase 2 — Manual edits required

Each item below has a TODO comment inserted **on the line above** the finding. Address each one before bumping `next` in `package.json`.

### `{BC-NNN}` — `{title}`

| File | Line | Recommended action |
|---|---|---|
| `{path}` | `{N}` | `{action}` |
| ... | | |

**Reference:** [`{section anchor}`]({upgrade guide URL})
**Codemod (where available):** `npx @next/codemod@canary {codemod-name} .`

(Repeat for each unique BC-NNN.)

---

## Phase 3 — Validation checklist

Behaviors that work in v15 without code change but **may produce different runtime output**, plus team-owned validation steps. Confirm each before declaring migration complete.

- [ ] **Run `npx @next/codemod@canary upgrade latest`** — covers async-request-API conversions, config renames, and many other items in one pass. Review the diff before committing.
- [ ] **`fetch()` default cache flipped from `'force-cache'` to `'no-store'`** (BC-101). If a call relied on default caching, add `{ cache: 'force-cache' }` and consider `next: { revalidate: N }`. Locations: `{file:line, file:line, ...}`
- [ ] **GET Route Handlers no longer default-cached** (BC-102). If a route handler relied on default static caching, add `export const dynamic = 'force-static'`. Locations: `{file:line, ...}`
- [ ] **Client Router Cache `staleTime` defaulted to 0** (BC-103). Page navigations no longer reuse a 30s in-memory route cache. If your UX assumed instant back/forward via Router Cache, set `experimental.staleTimes.dynamic` in `next.config.js`.
- [ ] **React 19 peer requirement** (BC-401). Bump `react` and `react-dom` to `^19.0.0`. Run `npm install` and address peer warnings. Pages Router still supports React 18 if a transitive blocker exists.
- [ ] **`next.config.js` validates** — config keys may have been renamed (BC-201, BC-202). Run `next build` locally to surface schema errors.
- [ ] **ESLint** — Next 15 ships with ESLint 9 by default. If the repo pins ESLint 8, set `eslint.useFlatConfig: false` or migrate the config.
- [ ] **Run `next build` locally before pushing.** Next 15's stricter type checking on async request APIs surfaces issues at build time.
- [ ] **Run the team's full test suite + smoke deploy.** This skill does not run tests — that is the team's gate.

---

## Recommended next steps (in order)

1. Review and merge the AUTOFIX edits from Phase 1.
2. Run `npx @next/codemod@canary upgrade latest` and review the diff.
3. Address every Phase 2 TODO. Tackle by BC-NNN class — fix all BC-001s in one PR, all BC-101s in another.
4. Bump `next` in `package.json` from `^14.x` to `^15.0.0`. Bump `react`/`react-dom` to `^19.0.0`. Run `npm install`.
5. Walk through Phase 3 checklist.
6. Run full test suite + smoke deploy.
7. Delete this `MIGRATION-PLAN.md`.

---

*This plan was generated by [`nextjs-modernizer`](https://github.com/DevExpGbb/zava-agent-config/tree/main/plugins/modernize-kit/.apm/skills/nextjs-modernizer) — a sibling of [`framework-modernizer`](../framework-modernizer/SKILL.md), part of the `modernize-kit` accelerator built with [Genesis](https://github.com/DevExpGbb/genesis).*
