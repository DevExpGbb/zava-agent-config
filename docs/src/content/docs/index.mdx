---
title: Zava IDP — Agentic SDLC marketplace
description: Internal Developer Portal for the Zava Agentic SDLC. Browse the catalog of skills, personas and instructions every Zava service repo pins via apm.yml.
template: splash
hero:
  tagline: The marketplace of agent skills, personas and instructions every Zava service repo pins via <code>apm.yml</code>. One change here propagates to every IDE, every PR, every coding-agent run.
  image:
    file: ../../assets/zava.svg
  actions:
    - text: Browse the catalog
      link: /zava-agent-config/catalog/
      icon: right-arrow
      variant: primary
    - text: Quick start
      link: /zava-agent-config/quick-start/
      icon: rocket
    - text: View on GitHub
      link: https://github.com/DevExpGbb/zava-agent-config
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';
import PolicyChip from '../../components/PolicyChip.astro';
import SDLCRibbon from '../../components/SDLCRibbon.astro';
import PluginGrid from '../../components/PluginGrid.astro';
import { VERSION } from '../../data/plugins';

<p class="lead-chip-row"><PolicyChip /></p>

## The SDLC ribbon — pick a phase

Filled cells ship a kit today. Dashed cells are roadmap (kits land additively, no breaking re-pins).

<SDLCRibbon />

## Phase kits — one per SDLC stage

<PluginGrid tier="phase" />

<div class="landing-two-col">
  <section>
    ## Foundation

    Every consumer pins this — the cross-cutting floor.

    <PluginGrid tier="foundation" />
  </section>
  <section>
    ## Accelerators

    Episodic, opt-in. Pin only while running a migration.

    <PluginGrid tier="accelerator" />
  </section>
</div>

---

<small>Marketplace version <code>v{VERSION}</code> · tag pattern <code>v&#123;version&#125;</code> · pinned by every consumer.</small>
