apm_modules/
.apm-cache/
.DS_Store
*.log
build/

# Deployed APM primitives — only relevant to consumer repos. This lib is a
# marketplace, not a primitives bag, so these dirs should never appear here.
# Kept in .gitignore as a safety net (e.g., if someone runs `apm install` after
# adding a transient dep during local debug).
.agents/
.github/agents/
.github/instructions/
