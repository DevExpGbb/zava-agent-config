apm_modules/
.apm-cache/
.DS_Store
*.log

# Deployed APM primitives — sources live in .apm/, regenerated by `apm install`.
# Library repo: don't commit derived copies (consumers do, libraries don't).
.agents/
.github/agents/
.github/instructions/
